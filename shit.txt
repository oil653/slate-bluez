/*
            Item{
                width: parent.width;
                height: 300;
                anchors.top: parent.top
                
                id: textRect 
                Repeater{
                model: Bluetooth.adapters
                    Text{
                        property var data: modelData;
                        anchors.centerIn: textRect;
                        text: BluetoothAdapterState.toString(data.enabled);
                    }
                }
            } */

            /*
            Item{
                width: parent.width;
                height: 300;
                anchors.top: parent.top
                
                id: textRect 
                ColumnLayout{
                    anchors.fill: parent;
                    Repeater{
                        model: Bluetooth.devices
                            Text{
                                property var data: modelData;
                                //anchors.centerIn: textRect;
                                text: data.deviceName;
                                font.pointSize: 30
                                color: data.connected == true ? "green" : "blue";
                            }
                    }
                }
            } */